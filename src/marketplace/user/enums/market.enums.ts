export enum MarketUserRole {
  seller = "seller",
  buyer = "buyer"
}

export enum BadgeType {
  VERIFIED = 'verified',
  TOP_SELLER = 'top_seller',
  FAST_RESPONDER = 'fast_responder',
  VERIFIED_BREEDER = 'verified_breeder',
  WHOLESALE_SELLER = 'wholesale_seller'
}

export enum AdminActionMarketType {
  BAN = "ban",
  WARN = "warn",
  EDIT = "edit",
  REMOVE = "remove"
}